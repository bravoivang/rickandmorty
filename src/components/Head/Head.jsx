import React from 'react';
import PropTypes from 'prop-types';
import './head.css';

const Head = props => {
    return (
        <div className="haed">
            Head
        </div>
    );
};

Head.propTypes = {
    
};

export default Head;